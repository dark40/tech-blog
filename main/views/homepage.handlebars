<!-- Loop over each object in the posts array -->
{{#each posts as |post| }}
  <div>
    <h2>{{post.title}}</h2>
    <p>Posted by {{user.username}} on {{format_date post.date}}</p>
    <p>{{post.content}}</p>
  </div>
{{/each}}
